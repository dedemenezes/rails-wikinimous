<div class="col-sm-8 col-sm-offset-2">
  <% @articles.each do |article| %>
    <article>
      <h2>
        <%= link_to "#{article.title}" %> <!--article_path-->
      </h2>
      <p>
        <%= article.content %>
      </p>
    </article>
  <% end %>
</div>